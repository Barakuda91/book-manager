const chai = require('chai');
const argv = require('minimist')(process.argv.slice(2));

describe('manager_test', () => {
    describe('book', () => {
        before(async () => {

        });
        it('mmStrategy', async () => {

        });
    });
});